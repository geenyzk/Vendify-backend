; APK releases are accepted by AppReleaseController up to 200 MB.
; PHP parses multipart bodies before Laravel runs, so these limits must exceed
; the controller validation limit or browsers receive a generic network error.
upload_max_filesize = 256M
post_max_size = 256M
max_execution_time = 300
max_input_time = 300
