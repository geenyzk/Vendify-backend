# Authorization

This API uses Bearer Token authentication. To authenticate, include the token in the `Authorization` header:

You can obtain your token by logging in and copying the token from your profile or API client.
